/** Tailwind CSS v4 PostCSS config. The single plugin replaces the v3
 *  `tailwindcss: {}` object; autoprefixer + postcss-import are built in. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
