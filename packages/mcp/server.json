{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.cbetz/last-ehr",
  "title": "Last EHR \u2014 Medplum",
  "description": "FHIR chart tools for MCP: read-only by default, with elicitation-gated, human-approved writes.",
  "websiteUrl": "https://www.lastehr.com/docs/mcp",
  "repository": {
    "url": "https://github.com/cbetz/last-ehr",
    "source": "github",
    "subfolder": "packages/mcp"
  },
  "version": "0.2.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@lastehr/mcp",
      "version": "0.2.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MEDPLUM_ACCESS_TOKEN",
          "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action).",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "MEDPLUM_CLIENT_ID",
          "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action).",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "MEDPLUM_CLIENT_SECRET",
          "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action).",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "MEDPLUM_BASE_URL",
          "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action).",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ]
}
